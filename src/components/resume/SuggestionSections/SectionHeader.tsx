
import React from 'react';
import { CardHeader, CardTitle } from '@/components/ui/card';

const SectionHeader = () => {
  return (
    <CardHeader>
      <CardTitle className="text-2xl font-medium">Resume Analysis Results</CardTitle>
    </CardHeader>
  );
};

export default SectionHeader;
